# Todo Agent 全量验收矩阵

最后更新：2026-08-10（macOS 首发版 0.0.1、当前授权档案、真实模型与飞书自动写回均已完成可见复验）

本表只记录可复现的验收项、测试任务前缀与结果；不记录用户任务内容、账号、Token、App Secret 或模型密钥。

## 执行规则

- 所有真实写入均只使用 `Codex验收-...` 前缀的测试任务。
- 本轮创建的本地验收任务均已移入本地可恢复回收站；飞书验收任务不做不可逆远端删除，除非用户在当时再次确认。
- 本地状态、飞书远端状态和 Agent 最终回复分别核对；`pending` 绝不写成“已同步”。
- 每个核心流程按新建 → 编辑 → 完成 → 重开 → 再完成（或等价可逆恢复）验证；真实 UI、隔离 E2E 与服务边界测试互为补充。

## 目标与证据

| 范围 | 验收点 | 状态 | 证据 |
|---|---|---|---|
| 本地任务 | 创建、搜索、Today/全部任务、详情字段、完成/重开、回收站/恢复、重启持久化 | 当前档案已验证 | 真实 UI 创建并编辑备注、项目、标签、高优先级、计划日；连续把计划日改为明天再改回今天，验证最终写入；完成、重开、再次完成、可恢复回收站与恢复均已走通。搜索后显式进入任何任务集合会清除临时查询，避免非零计数却显示空列表。 |
| 本地任务 | 原生开始/截止/提醒输入与竞态 | 自动化通过 | 保存失焦、字段修订号、全天/时间组合、连续编辑与重启持久化均有回归。当前档案的逐项手工重点是计划日；开始/截止的真实写回由 Agent→飞书独立验证。 |
| Agent | 真实模型、流式 Markdown、自然语言 CRUD、会话连续性 | 当前档案已验证 | 真实自托管模型在无 Key 模式下流式回复；主窗口创建、查询、改备注与优先级、完成、重开均通过。Markdown 表格、跨 Today/Agent 页面会话保留及失败状态可见。 |
| Agent → 飞书 | 写操作权限、可信同步回执、日期语义 | 当前档案已验证 | 对明确飞书来源的验收任务执行修改；收到可信“已同步”回执。开始时间 09:00、截止时间 18:00 独立写回，并在应用重启后保留。 |
| 飞书 | 新建、标题、备注、完成/重开、创建后自动同步 | 当前档案已验证 | 不点击“立即同步”：真实 UI 创建飞书任务，改标题与备注、完成、重开；每一步回到 `已同步`，且任务仍是同一远端记录。 |
| 飞书 | 负责人/关注人、全天语义、远端反向编辑、真实离线 | 自动化通过；当前账号未逐项手工操作 | 覆盖成员、全天、冲突、队列、重试与回拉；本轮不触碰非验收任务，也不改用户网络设置。 |
| 飞书 | 任务清单/项目映射 | 自动化通过；受权限范围限制 | 仅使用显式 `source.tasklist` binding；本地项目、标签、私人计划等不会被当作远端清单 GUID。 |
| 桌面体验 | 主窗口导航、浮窗置顶、内嵌聊天、返回路径 | 当前档案已验证 | 当前设置显示默认一秒悬停延迟；关闭主窗口后浮窗仍在；浮窗内可切换 Today/对话、显示 Markdown 并通过“打开主窗口”回到主界面。紧凑悬浮球/胶囊单击展开、双击直接回到 Today；右键提供 Today、快速录入、对话、隐私、位置锁定和设置；Dock/Finder 激活和二次启动会恢复并聚焦主窗口。 |
| 桌面体验 | 物理悬停离开、拖动、锁定、多显示器、全屏 | E2E 通过；实机边界保留 | 自动化覆盖悬停计时、提前离开、拖动/锁定、可读性与路由缓存；当前设备未进行物理多显示器/全屏实机验证。 |
| 视觉 | 浅/深色、窄/宽窗口、长内容、空/加载/错误状态 | 自动化通过 | 视觉专项覆盖 1180/980/760、深色/降低透明度、长 Markdown、窄浮窗表格、Toast 和无重复窗口外壳。 |

## 当前已知边界

1. 飞书 Task V2 `my_tasks` 列表只保证“我负责”的范围。列表暂时缺席不会被当作远端删除，产品已采取保守保护策略。
2. 没有全局飞书 Tasklist 选择器；飞书清单只能通过显式 `source.tasklist` binding 表示，不能由本地项目或列表猜测。
3. 当前未直接操作飞书客户端做反向日期编辑，也未在真实档案切断网络；对应字段、队列、冲突与重启场景由 Mock API 回归验证。
4. 当前 macOS 包未做 Developer ID 签名/公证；Windows 实机打包和多显示器/全屏观感仍需在对应设备补验。
5. 飞书验收记录因未获永久删除授权而保留；本地验收记录仅移入可恢复回收站。
