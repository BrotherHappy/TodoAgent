# Todo Agent 实施状态与验收说明

> 快照日期：2026-08-21<br>
> 对应版本：`0.0.1`<br>
> 文档用途：说明当前仓库已经具备什么、哪些仍需外部环境验证，以及哪些属于后续版本。产品目标仍以 [PRD.md](./PRD.md) 为准。

## 1. 状态口径

| 标记       | 含义                                                               |
| ---------- | ------------------------------------------------------------------ |
| 已实现     | 已有可运行代码和相应自动化测试，可进入人工验收                     |
| 待外部验证 | 协议与产品流程已实现，但必须使用真实账号、凭据、设备或签名环境验收 |
| 部分实现   | 核心链路可用，但尚未达到 PRD 对该模块的全部 P0 要求                |
| 后续版本   | 当前仓库未实现，属于 P1/P2 或移动端工作                            |

“已有代码”不等于“已经达到公开发布标准”。当前形态更适合定义为 **桌面端可验收 Alpha**；完成真实飞书、Windows 实机、签名分发、更多模型兼容矩阵及完整质量门禁后，才可升级为公开 Beta。

> v2.05 回归基线：未启动会弹窗的 Electron E2E；Todo Pet 小窝可选择六种本地陪伴性格，并可邀请最多 3 个只存在于小窝的房间伙伴，伙伴可点击互动并给出本地短句；有伙伴和开放任务时，小窝可用固定角色组成临时小队，准备后直接开始原任务专注或打开原任务；非仅宠物的紧凑桌面形态在天气开启且已有缓存时显示可点击天气胶囊，展示温度、状况、降水、缓存/严重提示并可打开既有天气卡片，隐私模式隐藏城市且不读取定位；时间线今天的日视图增加本地“现在”指示线和回到现在入口；设置新增可视化动作包编辑器，可从内置动作组合并安装启用安全声明式动作包；小窝新增共同收藏图鉴、共同旅程章节和共同完成印章，分别投影真实奖励库存、项目任务进度与真实完成任务事实；房间云灯、小植物和任务书架支持直接拖动、安全的位置微调、大小调整和恢复默认，旧宠物档案自动使用默认布局；小窝新增明亮日光、柔和灯光和月光夜色三档纯本地视觉氛围，并在开启季节小事件时投影四季内置装饰层；颜色主题包编辑器支持最多 12 个本机主题和四个 hex 颜色，并可加入 512 KB 以内 PNG/JPEG/WebP 背景、导出可读 JSON、导入预览后安装，严格拒绝脚本、路径、URL、SVG 和任意 CSS；Agent 可选跟随 Todo Pet 当前陪伴性格，或保持独立人格，旧设置默认开启联动；Vitest 单元/集成测试 129 个文件、804/804 通过，类型检查、生产构建和差异检查通过。此前已包含日历事件卡会后跟进任务预填与确认入口、时间线周视图估时复盘、主/备用模型价格档案、按提供方回报 token 的本地费用累计、每日费用硬上限、缺少可计费用量时的 fail-closed 覆盖、本机最多 8 个短期 Agent 会话归档/切换/搜索/重命名/置顶/删除，以及主 Agent 上下文快捷回答建议。

> v2.06 增量：菜单栏 / 系统托盘新增只读“今日待办”预览，读取同一份 Today 任务快照并显示最多 3 项未完成任务与总数；点击回到既有 Today 视图，隐私模式将标题脱敏，任务读取失败只降级菜单。新增托盘投影测试后，单元/集成测试基线为 130 个文件、808/808 通过；未启动会弹窗的 Electron E2E。

> v2.07 增量：小队一起开工新增临时领队选择；伙伴卡展示固定角色和协作提示，领队在准备/就位阶段承担第一步叙事。该选择只存在于当前陪伴交互，不写入宠物档案、不复制任务、不调用额外 Agent 工具；本轮在既有小队投影与组件测试中补充领队选择覆盖，单元/集成基线为 130 个文件、810/810 通过。

> v2.08 增量：小队开工卡新增可展开的协作简报；领队优先展示第一步，其余伙伴按固定角色顺序逐步点亮，准备结束后明确显示“协作简报完成”。该简报只属于本地陪伴层，不模拟 Agent 执行、不创建子任务、不调用额外工具、不改变任务或飞书状态；单元/集成基线为 130 个文件、812/812 通过。

> v2.09 增量：小队开工卡新增可展开的最多 3 项“小队路线”预览；路线从真实开放任务快照计算候选、预计时长和总时长，点击任一项只切换当前 UI 开工目标，不批量修改、不自动串联、不创建任务副本；单元/集成基线为 130 个文件、814/814 通过。

> v2.10 增量：成长首页新增“今日明信片”，从真实任务完成、已完成专注、今日待办和天气快照生成一次性陪伴文案与统计，可直接回到 Today；不持久化、不创建任务、不增加成长计数、不形成记忆、不写入飞书；单元/集成基线为 132 个文件、817/817 通过。

> v2.11 增量：天气服务在同一手工城市请求中读取最多 3 天结构化预报；小窝天气卡按真实 provider-local 日期展示今天/明天/后天的天气、低高温、降水与严重标记，旧缓存无预报时保持兼容，过期时明确标注缓存；不读取精确定位、不调用模型、不创建任务或改变飞书同步。本轮新增天气服务、纯投影与组件覆盖后，单元/集成基线为 134 个文件、821/821 通过。

> v2.12 增量：宠物本体现在依据结构化天气代码显示固定、轻量的雨伞/雪花/雷云闪电反应；晴朗或未知天气不添加装饰，缓存天气继续沿用过期标记。该反应不自动换装、不新增提醒、不读取定位、不改变任务、成长或飞书同步；新增天气映射与 PetCharacter 渲染覆盖后，单元/集成基线为 135 个文件、824/824 通过。

> v2.13 增量：自定义声明式动作包新增 8–60 秒动作冷却和 1–5 动作出现频率偏好；设置编辑器可直接调整，待机调度按确定性权重选择并保持最小间隔，旧 JSON 动作包缺失字段时沿用原有节奏。字段仍只引用内置动作，不执行脚本、网络或文件操作；全量 Vitest 为 135 个文件、826/826 通过，类型检查、生产构建和差异检查通过，未启动会弹窗的 Electron E2E。

> v2.14 增量：吸收 Desktop Pet / PawPal 的桌面层能力，Todo Pet 新增默认关闭的鼠标穿透。开启后宠物仍保持置顶可见，但点击会透传到后方窗口；主设置和系统托盘都能随时关闭恢复。设置迁移与导出兼容旧版本，穿透只改变浮窗命中，不改变任务、同步、Agent、专注或权限事实；本轮仍不启动会弹窗的 Electron E2E。

> v2.15 增量：吸收 Cat Fidget / BongoCat 的轻量输入映射，但收敛为隐私优先的系统空闲时间信号。设置新增默认关闭的“工作时回应”；主进程每 4 秒只读取系统空闲秒数，低频广播 `typing` / `reading` 姿态，浮窗用内置键盘和阅读动作表现，不读取具体按键、指针、窗口标题或输入内容。会议模式、隐私模式和宠物隐藏时不广播，旧设置与导入缺省为关闭；全量 Vitest 为 136 个文件、829/829 通过，类型检查、生产构建和差异检查通过，未启动会弹窗的 Electron E2E。

> v2.16 增量：吸收 Finch / Habitica 的“允许休假”但保持低压力边界。设置和宠物右键菜单新增休假模式；开启后只暂停宠物主动陪伴、工作时回应和启动时自动日记，任务、同步、专注、提醒和手动互动继续可用；旧配置与导入缺省为关闭，不维护连续签到或缺席惩罚。全量 Vitest 仍为 136 个文件、829/829 通过，类型检查、生产构建和差异检查通过，未启动会弹窗的 Electron E2E。

> v2.17 增量：吸收 Akiflow / Things Inbox 的“最近捕获回用”节奏；快速录入成功保存后在本机保留最多 12 条确认记录，快速录入窗支持回用与清空，宠物右键增加最近捕获入口。未确认的上下文预览不写入历史，历史不上传、不进入任务事件或飞书；新增纯函数和组件覆盖后，回归基线为 138 个文件、836/836 通过，未启动会弹窗的 Electron E2E。

> v2.19 增量：吸收 Notion AI / Taskade 的 Agent 可解释执行；主 Agent 对话新增短期“执行过程”卡，按可信运行事件显示工具拟定、等待确认、执行中、已完成、拒绝、失败、停止和复用结果，并可展开受权限引擎生成的影响预览。活动只保留在当前 renderer 运行中，不进入会话历史、模型上下文或飞书；主进程在权限拒绝时补发终态 `tool-finished(status=denied)`，避免 UI 把未执行的工具停留成拟定状态。回归基线以本轮全量命令输出为准，未启动会弹窗的 Electron E2E。

> v2.20 增量：延伸 Routine / Akiflow / Superlist 的上下文捕获回用；快速录入预览卡新增显式“保存到最近上下文”，支持保存剪贴板、当前窗口、选中文本和文本/链接拖入内容。本机最多保留 12 条、单条最多 2,000 字，支持回用与清空；未确认预览、文件/图片内容、窗口持续监听、Agent、飞书、任务事件和数据导出均不进入历史。新增纯函数与组件覆盖后，回归基线以本轮全量命令输出为准，未启动会弹窗的 Electron E2E。

> v2.20 回归补充：本轮新增的两个上下文历史测试文件共 4 个测试，和现有基线合计为 **142 个 Vitest 文件、850/850 通过**；下方长段中的 140/846 仅保留 v2.19 的历史叙述。v2.21 新增宠物小窗保存行为测试后，当前基线更新为 851/851。

> v2.21 增量：将最近上下文保存入口延伸到 Todo Pet 小窗的文本/链接拖入和选中文本预览；与快速录入共用本机最多 12 条、单条最多 2,000 字的历史，提供即时成功反馈，不自动读取文件正文、不进入 Agent、飞书或导出。新增宠物小窗接入覆盖后，回归基线为 **142 个 Vitest 文件、851/851 通过**，未启动会弹窗的 Electron E2E。

> v2.22 增量：延伸 Superlist Talk / OpenDesktopPet 的语音陪伴方向；主 Agent 与 Todo Pet 小窗的 Markdown 回复新增显式“朗读/停止朗读”，本机系统语音合成只朗读清理后的文字，代码块提示省略。默认不自动播放、不上传、不保存音频，系统不支持时按钮安全禁用；新增纯函数和语音适配覆盖后，回归基线以本轮全量命令输出为准，未启动会弹窗的 Electron E2E。

> v2.23 增量：把同一套显式本机语音入口延伸到晨间简报、Todo Pet 当前任务气泡和专注气泡。任务轮播或简报刷新时会停止旧文本；隐私模式只朗读“私人任务”提示，不读标题；默认不自动播放、不上传、不保存音频，所有入口共用一个可停止的语音通道。新增组件与切换行为覆盖后，本轮回归为 **144 个 Vitest 文件、855/855 通过**，未启动会弹窗的 Electron E2E。

> v2.24 增量：Agent 完成后的 Markdown 回复新增显式“保存到最近上下文”，以独立 `agent-reply` 类型复用本机最多 12 条、单条 2,000 字的历史；快速捕获和宠物入口可回用，流式回复禁用保存和朗读，不自动记录、不发送给模型、不写任务/飞书。本轮回归为 **144 个 Vitest 文件、857/857 通过**，未启动会弹窗的 Electron E2E。
> v2.25 增量：Agent 完成后的 Markdown 回复新增显式“复制 Markdown”，只在用户点击后写入系统剪贴板，便于迁移到文档或 Issue；流式/空回复和不支持剪贴板的环境禁用，复制失败不影响文字回复，不上传、不进入任务、飞书或最近上下文。本轮回归为 **144 个 Vitest 文件、858/858 通过**，未启动会弹窗的 Electron E2E。
> v2.26 增量：最近上下文历史接入主 Agent 和 Todo Pet 聊聊入口；点击历史条目只把本机原文追加到输入框并等待用户确认发送，历史保存/清空通过窗口事件即时刷新，不自动发送、不重复保存、不扩大模型数据范围。本轮回归为 **144 个 Vitest 文件、859/859 通过**，未启动会弹窗的 Electron E2E。
> v2.27 增量：主 Agent 完成回复新增显式“提取行动项”；确定性解析只接受明确 cue/清单/动作句，最多生成 8 个本地草稿，确认面板支持编辑、逐项取消和一次性创建本地任务；网页研究内容不会自动执行或直接写回飞书，未识别时不打开空面板。本轮回归为 **145 个 Vitest 文件、864/864 通过**，未启动会弹窗的 Electron E2E。
> v2.28 增量：主 Agent 在任务已选中时新增显式“保存研究卡”；确认面板预填并允许编辑标题、可选安全来源、原始 Markdown 摘要和行动项，确认后复用任务更新写入本地私人 `researchCards`，并在父层与组件层双重校验任务存在、卡片/行动项数量、字段长度和 URL；流式回复、无选中任务、任务切换或超限均不保存，研究卡不写回飞书、不自动执行网页指令。本轮新增 Agent Markdown 与研究卡面板覆盖，回归基线以全量命令输出为准，未启动会弹窗的 Electron E2E。
> v2.29 增量：任务详情展开研究卡时将 Markdown 摘要以可读排版展示标题、列表、表格、引用、代码块和安全来源链接；原始 HTML、远程图片和不安全协议降级为文本，表格支持独立横向滚动，渲染只读且不执行研究内容、不写回飞书。本轮新增研究卡 Markdown 渲染与安全边界覆盖，回归基线以全量命令输出为准，未启动会弹窗的 Electron E2E。
> v2.30 增量：研究卡每条行动项新增显式“创建任务”入口；创建复用普通本地任务控制器，保留研究卡、来源链接和原任务 provenance，继承私人项目/计划日期但固定本地来源；当前详情会话中成功项禁用重复入口，失败可重试，不自动创建、不写回飞书。本轮新增行动项任务草稿与组件覆盖，回归基线以全量命令输出为准，未启动会弹窗的 Electron E2E。

> v2.31 增量：任务详情新增“保存为工作流模板”入口；确认面板允许编辑名称和说明，将当前任务的备注（仅限受支持变量）、标签、优先级和有效预计时长转换为一个 `defaultSource: local` 步骤；完成状态、具体日期、附件、同步身份和 Feishu 外部 ID 不复制，模板继续通过 Quick Capture 的预览链路使用，不执行脚本、不后台运行、不写回飞书。本轮新增模板生成与保存面板覆盖，回归基线为 **150 个 Vitest 文件、879/879 通过**，未启动会弹窗的 Electron E2E。

> v2.32 增量：保存任务模板时可选择带入最多 11 个子任务；模板步骤增加受限 `parentStepId`，仅允许引用前面已定义的步骤。Quick Capture 创建本地模板时按已创建步骤 ID 恢复父子关系，选择飞书来源时不会把本地任务 ID 发送到远端；面板展示步骤数量和子任务预览，可取消带入。本轮新增父子模板、校验和来源边界覆盖，回归基线为 **150 个 Vitest 文件、881/881 通过**，未启动会弹窗的 Electron E2E。

> v2.33 增量：任务列表批量选择新增批量编辑属性面板，可统一修改优先级、项目、清单和标签（替换/追加/移除）；编辑先生成预览，主进程在一次事务内校验所有任务基线并记录单一可撤销 `bulk` operation；这些属性只属于本地上下文，飞书任务保持同步状态不变，删除任务不可参与。本轮新增编辑面板、原子服务、撤销、校验和飞书边界覆盖，回归基线为 **151 个 Vitest 文件、885/885 通过**，未启动会弹窗的 Electron E2E。

> v2.34 增量：本地循环任务详情新增“跳过本次”；主进程在同一事务内用下一次循环锚点平移日期、开始/截止时间、时间块和提醒，保持任务 ID 与系列不变并记录单一可撤销 `skip-recurring` operation；不创建重复任务，飞书循环、专注中的任务、无日期锚点和最后一次循环均明确拒绝。本轮新增服务、IPC、控制器、详情入口、导入/Markdown 历史兼容与边界测试，回归基线为 **151 个 Vitest 文件、888/888 通过**，未启动会弹窗的 Electron E2E。

> v2.35 增量：Agent 任务工具新增 `task_skip_recurring`；分析阶段展示当前锚点、下一次日期、保持任务 ID、无远端写入和可撤销性，执行阶段重新读取并调用任务服务，飞书循环、专注中、无日期或最后一次循环 fail-closed。本轮新增 Agent 预览/执行/来源边界覆盖，全量回归基线为 **151 个 Vitest 文件、889/889 通过**，未启动会弹窗的 Electron E2E。

> v2.36 增量：吸收 OmniFocus Flagged、Taskwarrior urgency/tag 和 Todoist 的重点筛选习惯；任务增加独立的本地“重点标记”，任务行/详情可切换，筛选面板、一句话筛选、保存视图、Todo Pet 视图、批量编辑和 Agent 任务工具均可使用。标记不改变优先级、不进入飞书共享 payload；安全 JSON 与可读 Markdown 保持该字段，旧任务默认未标记。本轮全量回归基线为 **151 个 Vitest 文件、895/895 通过**，未启动会弹窗的 Electron E2E。

## 2. 当前实施概览

| 模块                | 状态       | 当前可用范围                                                                                                                                                                                                                                                                                                                                                           | 仍需完成                                                                                                                                                            |
| ------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 桌面外壳与视觉      | 已实现     | Electron + React 三窗口；Todo Pet 已成为唯一桌面悬浮形态，旧悬浮球/胶囊已从运行路径和设置移除；支持置顶、自由拖动、多显示器位置记忆、75–125% 缩放、悬停展开、离开收起、单击互动、双击主应用、右键快捷菜单、可从右键或托盘进入/退出 Boss Mode、默认关闭的鼠标穿透（设置/托盘可恢复）、焦点记忆和 Today 上下轮播暂停；主应用提供完整小窝；主窗口新增 `⌘/Ctrl+K` 命令面板，支持关键词过滤、上下键选择、Enter 执行和 Esc 关闭 | Windows 实机、多显示器、全屏应用、系统缩放、高对比度和 200% 字体人工验收 |
| 本地任务核心        | 已实现     | 本地持久化、增删改查、完成/重开、回收站/恢复/彻底删除、撤销、Inbox/Today/Upcoming/All/Completed、搜索/筛选（含附件/链接/自定义字段、研究卡元数据与讨论正文）、循环与系列范围、循环任务“跳过本次”（同一任务平移日期/时间块/提醒并可撤销）、私人 Today、优先级/重点标记/项目/标签/手动情境、项目总览、独立项目与清单实体（新建、重命名、归档、恢复、删除并解除任务关联）、子任务/依赖、依赖链解释卡（前置/当前/后续节点、缺失/循环提示）、命名上下文链接、自定义字段（文本/数字/日期/链接/勾选）、可折叠研究卡（来源/摘要/行动项，Agent 回复可在选中任务后编辑确认保存）、本地文件附件选择/复制/打开/预览/移除、http/https 附件引用、最多 100 条本地讨论（添加/编辑/删除/撤销）、任务列表批量选择与预览确认（完成/恢复/安排到今天/回收站/批量编辑重点标记、优先级、项目、清单、标签，单次撤销）、暂存逐项整理（今天/明天/稍后/完成/打开详情和 1/2/S/C/O 快捷键）、提醒、专注计时、草稿恢复、可保存筛选视图、优先级原因提示、任务详情脱敏历史时间线，以及基于本地时间块的桌面日/周时间线（日视图半小时刻度、今天当前时刻指示与定位、拖动重排、未安排托盘、工作周期 1/2 周容量预测、项目三列看板、周概览和完成/专注/逾期回顾、项目健康卡）；今日规划支持保守 / 平衡 / 冲刺三种容量策略，并在同一确认单中保留固定事项、解释原因和单次撤销；确认前另有只读时间块预览，固定已有时间块、按窗口与缓冲排布其余任务并显示冲突；会议和 Agent 回复行动项提取预览复用同一任务创建链路，可逐项编辑/勾选后批量创建本地任务                                                                                                                                 | 附件预览目前限受控文本与 PNG/JPEG/GIF/WebP；PDF、Office、压缩包等仍回退系统打开；历史仅覆盖本地可撤销操作，讨论、研究卡和附件搜索只覆盖本机已保存内容；手动情境不读取定位且不写回飞书；周期候选仅为只读建议，不自动改期；时间块预览不会静默写入 `timeBlock`；远端变化以同步状态呈现；性能与崩溃恢复压力测试待做                                                                               |
| 快速捕获            | 已实现     | 快捷窗、中文日期/时间/标签/提醒/手动情境/预计时长/循环解析（`@办公室`、`情境：办公室`、`预计 45 分钟`、`30m`、`每天`、`每周一`）、Todoist 风格 `p1`–`p4` 优先级、解析 Chip、本地或飞书来源选择、草稿保留；剪贴板文字、当前窗口应用/标题和选中文本都通过显式按钮或全局快捷键读取，先显示有长度上限的上下文预览，再由用户带入输入框；快速窗和宠物小窗支持显式语音输入，识别结果只进入可编辑草稿；内置“会议跟进 / 研究简报 / 发布文章”工作流模板，支持设置中导入受限 JSON 模板，保存前展示步骤并逐项创建；解析后可显式选择保存为普通任务、无日期/项目/提醒的本地暂存或 Todo Pet 日记，日记捕获 ID 重试幂等；Todo Pet 任务栏直接复用解析器创建本地任务，解析失败回退纯标题且不会隐式写入飞书；macOS 使用前台应用查询与受限复制桥接，Windows 使用受限前台窗口 API，均有超时、剪贴板恢复和不可用降级                                                                                                                                                                                                                                                                                           | 更多自然语言歧义、时区、跨午夜与 Windows 输入法回归测试；不同应用的辅助功能权限、富文本剪贴板恢复、模板批量失败恢复和持续上下文历史仍待后续                                                                                                             |
| 提醒与晨报          | 部分实现   | 系统通知、应用内回退、到期提醒、完成/10 分钟/1 小时/明天/自定义时间稍后、打开动作、安静时段、右键“安静一小时/今日安静”、漏发合并、去重、睡眠唤醒刷新；普通任务支持可调整的每日提醒预算（默认 8 次）、关闭同一提醒两次后的降频、不同任务最小间隔，以及本地 / 飞书来源级“全部 / 仅高优先级 / 关闭”策略和最多 100 个项目例外（项目策略优先）；设置页已支持最多 100 个项目的批量策略编辑；晨报、同步风险、Agent 审批走独立重要通道；Todo Pet 主动陪伴有独立每日预算（默认 2 次），主进程最终限流；晨间简报新增可跳过的三步启动卡，可选最多 3 项、容量和先专注/先看规划，并把预选值带入既有规划弹窗；专注结束和结构化严重天气通知遵守横幅、静音与安静时段；AI 不可用时回退事实型晨报 | 跨设备预算同步仍待补齐 |
| Todo Pet 动作、专注与成长 | 已实现     | 20 种待机动作、8 种情绪与任务/专注/同步/Agent 动作组；头、眼、耳、尾和四肢独立运动，持续呼吸、随机眨眼与鼠标视线跟随；头/腹/手/下半身点击反馈；内部任务拖给宠物后可落到“专注 / 完成 / 稍后”目标区，也可在气泡内完成、移到今天或专注；文本、URL、文件和图片拖入后只显示预览，可手动带入聊聊；右键互动、九项径向互动轮盘与快捷回答；阅读/写作/开发/调研/沟通/运动/整理主题动作包会按任务标题、备注和标签本地推断，只改变宠物姿态和气泡标签；支持安全的声明式 JSON 动作包安装、启用、更新和卸载，设置新增可视化编辑器，可载入当前包、从 20 个内置待机动作组合节奏、调整 8–60 秒冷却和 1–5 出现频率并安装启用；编辑器和 JSON 入口共用字段/动作白名单，不能携带脚本、网络请求、文件操作或外部素材路径；工作时回应为默认关闭的本地输入节奏反馈，主进程只用系统空闲秒数低频触发敲键盘/阅读姿态，不读取具体输入内容；小窝新增共同收藏图鉴、共同旅程章节和共同完成印章，按真实库存与任务事实显示解锁、项目进度、最近完成记录和下一步入口；房间摆件支持直接拖动、本地位置微调、75%–130% 大小调整和恢复默认，旧档案缺少布局时自动投影默认坐标；小窝提供明亮日光、柔和灯光和月光夜色三档纯本地视觉氛围；Agent 可选跟随宠物当前陪伴性格，或使用独立 Persona；四套动作性格和两档幅度；25/5、50/10、90/20、自定义番茄与正计时；专注阶段可选本地生成的轻雨、林间、咖啡馆或白噪音，暂停/休息/结束自动静音且默认关闭；小窝提供“今日进展 / 今晚回顾”事实卡、可选每周 Check-in（能量 / 节奏 / 备注）；等级、亲密度、五项属性、三种小房间、四套配色/服装、摆件、最多 3 个房间小伙伴、每日冒险、共同日记（可回看关联任务）、可关闭的季节装饰事件（春花/夏帽/秋叶/冬围巾）、可控记忆、两种休息小游戏和小窝首页的可配置弹性习惯（最多 12 项，可编辑/暂停/完成/稍后/移除）均原子或本地持久化；宠物回顾新增主动开始的逐项处理会话，完成/安排今天/专注/稍后均复用任务控制器；鼠标穿透为独立本地浮窗开关，不影响宠物动作和任务事实 | 第三方连接器和网络主题市场仍属后续增强 |
| Todo Pet 天气       | 已实现     | 手工城市、无需精确定位；结构化当前温度、体感、高低温、降水概率、最多 3 天预报、30–120 分钟缓存、过期标记与断网保留；宠物本体按代码显示固定雨伞/雪花/雷云闪电反应；雷暴等严重代码使用确定性映射 | 供应商冗余、旅行城市、官方预警详情和 Windows 通知实机待后续验证 |
| 飞书任务            | 待外部验证 | 默认 `personal-direct` 零服务器一键连接；另有 `existing-direct` 可安全保存已有应用 App ID/Secret、跳过注册并复用 Device OAuth；App Secret 与 Token 进入系统安全存储，浏览器阶段状态、取消、到期和脱敏错误已接入；飞书 Task v2 客户端、Token 刷新、分页、退避、离线持久队列、公共字段双向同步、三方合并、冲突处理、远端删除与私人计划隔离；Relay 与本机回调开发模式保留为兼容路径 | 必须使用真实飞书账号分别完成专属应用和已有审核/发布应用的 Device OAuth、Token 刷新及 Task v2 读写；租户策略、多角色权限矩阵和逐字段冲突仍待外部验收。Relay 不是默认门槛，仅在准备支持该高级路径时另行验证 |
| AI 对话与任务 Agent | 待外部验证 | AI 可选关闭；OpenAI-compatible Chat Completions；主模型、本地备用模型与 local-only 路由；备用切换只发生在网络/408/429/5xx 且尚未产生流式内容时；API Key 进入系统安全存储；连接测试、受控网络重试、每日 Token 硬预算与用量展示；设置可分别填写主/备用模型输入与输出单价，本地按提供方回报的 token 累计费用并在达到每日费用上限前阻止新运行，缺少可计费用量时安全停止；每日一次的只读 AI 晨间总结与手动刷新；自然语言单条和显式批量任务 CRUD、2–7 步任务拆分预览与本地子任务创建、研究卡读取与新增、提醒、移到 Today、可恢复删除及撤销；任务列表每行和批量选择工具栏可一键把标题、ID、来源和状态带入 Agent 草稿但不会自动发送或写入，批量上下文最多内嵌 20 项；严格模型数据裁剪；已使用真实 `DeepSeek-V4-Flash-0731` 验证连接、原生 `tool_calls`、查询、创建、批量更新确认、晨报和用量统计 | 仍需扩展更多兼容端点，并在真实提供方环境验证 429、5xx、长会话和停止；费用核算依赖用户填写价格与提供方回报 prompt/completion tokens，不代表提供方账单，也不自动猜价                         |
| Agent 短期会话 | 已实现 | Agent 页面默认将每个会话最近 50 条非流式消息只保存在本机，最多保留 8 个会话，支持切换、按标题/正文搜索、重命名、置顶、删除、新建、清除当前会话和导出 Markdown；主 Agent 页面按当前任务事实显示只读优先快捷回答建议；恢复的对话仍受 `modelDataScope.chatHistory` 控制，关闭该范围时不会送给模型，也不会自动形成关系记忆 | 跨设备会话和云端同步仍待后续 |
| Agent 权限与审计    | 已实现     | R0–R4 确定性权限判断、结构化影响计划、单次批准、精确范围临时全权限、全局停止、执行前二次校验、密钥/日志脱敏、追加式审计与动态详情/导出；锁屏/睡眠立即撤销全权限；研究结果与后续行动强制分轮确认；权限中心新增任务读写、飞书同步、网页研究、文件与终端、剪贴板与屏幕五层独立能力开关，工具注册表在模型暴露、审批准备和执行前均重检，关闭飞书时远端任务读取/写回 fail-closed 且不降级为本地写入                                                                                                                                                                        | Windows 本机身份验证和完整攻击面测试仍需实机验收；R4 及审计隔离需在签名打包产物中复验                                                                               |
| Agent 通用工具      | 部分实现   | 任务工具；限定工作目录的文件读/列出/搜索/写/移动/删除；受约束命令运行；剪贴板；截图；打开 URL；受限 HTTP 读取；公开网页搜索；所有外部内容按不可信输入处理；五层能力开关会按工具族过滤模型可见列表并在运行中撤权                                                                                                                                                                                                              | 当前不是独立 Agent 浏览器，也不是容器级终端沙箱；目录由产品固定而非用户选择；登录态浏览器、桌面应用控制、连接器、MCP、Skills、子 Agent 和无人值守工作流属于后续阶段 |
| 数据管理            | 已实现     | 任务、设置与审计支持安全导出、导入预览、跳过/覆盖/复制策略和选择性清除；凭据不进入导出，审计保持追加只读；Markdown 默认是可读任务清单，可选追加不含快照的任务事件摘要；Todo Pet 另提供 `.todo-pet.json` 档案备份、导入预览与跳过/覆盖策略，迁移成长、外观、库存、可配置弹性习惯、本周同行目标、最多 3 个房间伙伴、冒险、小游戏、日记、记忆、主动消息和专注历史；旧档案缺少 habits/goals/companions 时补默认值，导入永不带入正在运行的专注 | 大数据量、异常中断、跨版本恢复演练及跨设备云同步仍待补齐 |
| 安装与发布          | 部分实现   | 已用本轮源码重新生成 macOS arm64 DMG/ZIP；当前构建已安装到 `/Users/hx/Applications/Todo Agent.app`，完成临时签名校验、主进程与双渲染进程启动冒烟；上一安装版本保留为可恢复备份；Windows x64 ZIP 和 NSIS x64 脚本沿用此前构建基线 | macOS Developer ID 签名与 notarization、Windows 代码签名、NSIS 安装器的 Windows CI 构建、安装/升级/卸载矩阵、自动更新和 Windows 实机验收尚未完成 |
| iOS / Android       | 后续版本   | 任务模型、来源、同步状态与权限语义已为移动端复用预留                                                                                                                                                                                                                                                                                                                   | 尚无移动客户端、移动同步后端、登录、推送、离线冲突、Widget/Live Activity、移动端审批或商店发布工作                                                                  |

跨日规划补充：`src/shared/multi-day-schedule.ts` 与 Today 规划面板提供未来 5 个工作日的只读顺延投影；它尊重固定时间块、工作日、截止日期和规划窗口，不能替代用户在时间线中的实际改期操作，也不进入飞书同步字段。

专注节奏补充：`src/renderer/focus-insights.ts` 从任务本地专注记录投影 7 日投入、专注段数、平均时长和投入最多任务，时间线周视图只读展示并支持回到原任务，不改变任务或飞书字段。

下一步建议补充：v1.57 将 Taskwarrior 风格的 urgency 收敛为 `AppSettings.planning.urgencyWeights`；通用设置可调整截止、Today、优先级和短任务四项 0–100 权重，Todo Pet 使用同一确定性评分并继续显示文字原因，设置迁移和导入缺省回退默认值，权重不进入任务或飞书。

陪伴策略补充：v1.58 新增 `src/renderer/companion-presets.ts`，设置可一键应用温和陪伴、自然平衡、深度专注或活泼互动；模板只组合既有设置，用户逐项修改后显示自定义，不新增任务或同步数据。

命令面板补充：v1.65 吸收 Amie / Routine 的 `⌘/Ctrl+K` 命令菜单模式，新增 `src/renderer/CommandPalette.tsx`。面板将搜索任务、快速捕获、Today/暂存/全部、今日规划、Todo Pet、Agent、飞书同步、显示宠物和设置集中为可搜索命令；上下键循环、Enter 执行、Esc/遮罩关闭，弹窗期间隔离返回和新建快捷键。命令只调用既有导航、控制器和同步 API，不持有第二份任务状态，也不绕过确认链。

晨间启动补充：v1.66 吸收 Sunsama / Motion 的轻量 morning kickoff，新增 `src/renderer/MorningKickoffCard.tsx`。卡片以三步临时收集最多 3 项重点、1/2/4/6 小时容量和“先专注 / 先看规划”意图；先看规划只把 `MorningKickoffPreset` 带入既有 `DailyPlanSheet`，先专注复用 `TaskController.startFocus`，卡片本身不新增 IPC、不写任务、不触发飞书同步。

Boss Mode 补充：v1.67 吸收 Desktop Goose / Shimeji / Weyrdlets 的安全桌面入口，在 `src/shared/boss-mode.ts` 统一把会议模式与宠物隐藏合并为一次设置更新；宠物右键菜单和系统托盘共用同一开关，退出 Boss Mode 会重新显示宠物，任务、专注、飞书同步和系统安全通知不受影响。

日历议程补充：v1.68 吸收 Structured / Any.do / Sunsama / Morgen 的任务与日历同屏模式，新增 `src/shared/calendar-events.ts` 与 `src/renderer/calendar-store.ts`。时间线日视图可从本机选择 `.ics` 文件，解析并合并最多 500 个事件，显示当天来源、时间、全天/跨午夜裁剪结果；用户可以清空并通过通知撤销。`DailyPlanSheet` 将当前可用时段内的日历忙碌分钟从容量扣除，并把会议作为只读保留区安排任务，所有内容只存在本地，不写回外部日历、飞书或任务共享字段。

晨间日历摘要补充：v1.69 新增 `src/renderer/morning-calendar.ts`，Today 的晨间简报从同一缓存显示当天事件、事件数量和占用总时长，并提供打开时间线入口；无事件时不显示空卡片。该投影不调用模型、不创建任务、不写入外部日历。

周视图日历负载补充：v1.71 让 `TimelinePage` 的周概览按七天聚合同一本地日历缓存，在有事件的日期标题和周摘要显示会议数量、占用时长；点击日期仍进入完整日议程，聚合只读、不创建任务操作、不写回日历或飞书。

会后跟进补充：v1.85 为日视图事件卡增加“跟进”按钮，`calendar-follow-up.ts` 只生成本地草稿，`NewTaskSheet` 预填标题、日期和来源/时间上下文；用户关闭或编辑后仍由既有任务创建流程确认，默认不写入飞书、不修改日历。

估时复盘补充：v1.86 扩展 `focus-insights.ts` 的周投影，使用 `estimatedMinutes` 与本周 `focusSessions`/兼容聚合字段生成预计、实际、偏差和最多 4 项偏差任务；`TimelinePage` 只读呈现，点击仍回到原任务，不创建新计数、不写任务、不写回飞书。

会议行动项补充：v1.87 为 `CalendarEvent` 增加受限 `description` 字段（`.ics` `DESCRIPTION` 最多保留 4,000 字符），由 `calendar-action-items.ts` 纯函数识别显式行动项/待办/下一步段落、清单和强动作动词，去重后最多生成 8 项草稿。时间线事件卡打开 `CalendarActionItemsSheet`，用户可以逐项编辑标题、取消选择并一次确认创建本地任务；创建顺序复用 `TaskController.create`，部分失败保留预览并提示已创建数量，绝不调用模型、修改日历或写回飞书。

日历忙碌区补充：v1.88 在 `calendar-events.ts` 增加 `calendarBusyBlocksForSlot`，把已按本地日裁剪的 `CalendarBusyBlock` 投影到半小时时间格；`TimelinePage` 使用不可交互的背景层标出会议区，只在事件开始格显示标题，继续允许任务卡拖动和投放。它与 `DailyPlanSheet` 共享同一忙碌块事实，不新建事件、不占用同步字段。

宠物人格补充：v1.89 将 `PetProfile.personality` 作为本地持久化的六种预设（`gentle` / `energetic` / `calm` / `playful` / `witty` / `quiet`），小窝提供切换入口；`PetCharacter` 以可访问标签、数据属性和不同呼吸/尾巴/耳朵节奏表达性格，浮窗、主窗口、小窝和小游戏读取同一份 profile，旧版缺失或短暂预发布别名会安全回退到温柔陪伴。性格不改变任务事实、提醒预算、权限或飞书同步。

Todo Pet 智能视图补充：v1.73 吸收 Todoist / Remember The Milk 的“保存查询可复用”模式，新增 `src/renderer/pet-smart-view.ts`。宠物“全部”任务面板可选择主窗口已保存的视图，复用项目、标签、情境、日期、来源和排序条件；视图只过滤同一份开放任务快照，首次默认为全部任务，视图被删除或失效时回退，不复制任务、不创建操作、不改变飞书字段。

Todo Pet 档案迁移补充：v1.74 吸收 Vikunja / Joplin / Anytype 的本地优先迁移思路，新增独立 `.todo-pet.json` 备份格式。设置 → 数据与安全可导出或选择档案导入；导入先读取一次并展示成长、库存、冒险、小游戏、日记、记忆、主动消息与专注历史的数量，再选择保留本机或覆盖本机。档案不包含凭据、活动专注或本地文件路径，覆盖通过 PetService 原子状态写入并递增本机修订号；不会写入任务、飞书或 Agent 审计链。

弹性习惯补充：v1.75 将原本 renderer localStorage 中的三个固定提示迁移为 PetService 管理的最多 12 项习惯。小窝首页可新增、编辑、暂停/恢复、完成、稍后或移除，间隔限制为 15–1440 分钟；旧数据只迁移一次，写入成功后才记录迁移标记。习惯纳入 `.todo-pet.json` 档案，既不创建任务、奖励或连续签到，也不进入飞书同步和模型上下文。

每周同行目标补充：v1.76 吸收 Sunsama Weekly Objectives、Finch Goal Challenges 与 Amazing Marvin Goals，在小窝首页加入最多 3 项可选的本周目标卡。目标可追踪完成任务、专注分钟或习惯照顾，进度从真实任务/专注历史/习惯快照实时投影；支持新增、编辑、暂停/恢复、移除和档案迁移，不复制任务、不维护第二份计数、不制造连续签到或惩罚，也不写回飞书或进入模型上下文。旧档案缺少 `goals` 时自动补空数组。

专注守护补充：v1.77 吸收 Focus Friend Focus Shield 与 Forest 的专注保护思路，在设置中加入关闭 / 温和提醒 / 匹配时自动暂停三档与最多 12 个应用名片段。Todo Pet 专注期间通过既有跨平台前台窗口桥接仅读取应用名，不读取窗口标题或内容，不关闭、阻挡或控制外部应用；匹配后显示可折叠守护气泡，自动暂停只复用现有 Pet/Task 专注 API。应用名配置、匹配和导入校验均有 Vitest 覆盖，默认关闭。

引导式回顾补充：v1.78 吸收 OmniFocus Review、Things Logbook 与 Microsoft To Do Suggestions 的逐项决定节奏。`PetReviewCard` 保留逾期、依赖阻塞、待排时间的只读事实投影，用户主动点击“开始回顾”后由 `PetReviewSession` 快照当前队列，每次只呈现一项并可完成、安排今天、开始专注、打开任务或稍后；会话状态不持久化、不创建副本，错误留在当前项，所有写操作复用任务控制器、撤销和飞书同步。

模型费用预算补充：v1.79 延伸 AppFlowy / Super Productivity 的本地用量治理。设置分别保存主模型与备用模型的输入/输出单价（美元 / 百万 tokens），`ModelUsageBudgetService` 只在提供方回报对应 token 维度时累计本地费用；达到每日费用预算或已配置价格但无法可靠计费时，新的模型运行安全停止。用量文件只保存按本地日汇总的 token、费用和计数，不保存提示词、回复、模型名、Endpoint 或凭据；导入导出保留价格偏好但仍剔除凭据引用。

之前计划建议补充：v1.70 新增 `src/renderer/morning-rollover.ts`，Today 晨间简报从全部开放任务中找出较早日期的未完成私人 Today 计划，最多显示 3 项并提供“安排今天”、打开全部任务和稍后再看。安排动作复用既有 `moveToToday`、同步队列和撤销；默认不自动 rollover，不改变截止日期、共享字段或飞书 payload。

任务行 Agent 入口补充：v1.59 新增 `src/renderer/task-agent-context.ts`，任务列表行可将标题、稳定 ID、来源和状态带入 Agent 草稿；入口只导航和预填，不自动发送、不绕过权限，批量选择时隐藏，后续仍沿用任务详情查询、影响预览、确认、审计和撤销。

批量 Agent 助手补充：v1.60 复用同一 helper 增加批量选择工具栏入口，最多内嵌 20 项所选任务，超出部分只保留数量提示；草稿要求 Agent 先逐项查询、解释目标和影响，不明确时先追问，任何完成、删除、改期或同步都不能绕过既有确认链。

Filter Assist 补充：v1.61 新增 `src/renderer/smart-view-query.ts`，筛选弹窗支持一句话解析日期、优先级、来源、项目、标签、情境和排序；解析只使用当前快照已有候选，先展示摘要再套用，未知值、冲突、空语句和超长语句均拒绝，不调用模型、不写任务。

可读事件摘要补充：v1.62 在 Markdown 导出旁增加“包含事件摘要”开关；开启后由 `DataPortabilityService` 从已有本地 operation log 生成按时间排序的摘要，只输出操作类型、任务标识、字段名和撤销标记，不输出 before/after 快照、私人路径或凭据，默认关闭且不影响 JSON 恢复备份。

快速捕获去向补充：v1.63 吸收 Superlist 的“任务 / 笔记同上下文”捕获模式，在解析预览下新增“任务 / 暂存 / 日记”。暂存创建真正没有计划日期、截止、提醒和项目的本地任务；日记走独立 `pet:diary-from-capture` IPC，只保存标题与用户原文，使用 captureId 防重复，不创建任务、不写飞书。

宠物输入补充：v1.64 让 Todo Pet 任务栏调用同一 `capture:parse` 解析器，输入可携带日期、标签、情境、预计时长、循环和 `p1`–`p4` 优先级；解析结果写入本地任务，解析失败回退原始标题。即使文本含“存到飞书”，宠物小窗也固定使用本地来源，不隐式触发远端写入；成功与失败都会以宠物气泡反馈。

> 2026-08-20 增量：本轮新增 Todo Pet 只读回顾队列（逾期 / 依赖阻塞 / 待排时间）与小窝导航承接，并把前置依赖编辑、自定义字段类型、项目总览、独立项目与清单实体（新建 / 重命名 / 归档 / 恢复 / 删除并解除关联）、命名上下文链接、本地文件附件、http/https 附件引用、受限附件预览、任务详情脱敏历史时间线、任务本地讨论、项目三列看板和任务列表批量操作（精确选择、预览确认、原子执行、单次撤销）接入任务体验；TaskService 对新增/编辑统一做循环防护，缺失依赖保留为可解释阻塞。悬浮宠物在设置热更新和跨测试窗口指针切换时会恢复正确的悬停计时。附件服务新增主进程文件选择、复制、打开、预览、删除、孤儿清理和清空数据联动，单文件 25 MiB / 单批 100 MiB 上限并拒绝越界符号链接。飞书错误状态也已统一映射到主窗口标题栏、侧栏和同步页，不再用“已连接”覆盖权限或不可重试失败。完整测试数量以本轮最终回归命令为准。

> v1.33 增量：保存视图新增标签与日期范围条件（逾期、今天、未来 7 天、无日期），旧版本地视图读取时自动补齐默认条件；筛选只投影当前任务快照，不改变任务事实或飞书同步载荷。

> v1.34 增量：父任务行与任务详情新增子任务完成进度投影；操作子任务时保留父任务详情上下文，父任务不会被隐式完成，删除子任务不计入总数；未新增同步字段或飞书写回载荷。

> v1.35 增量：保存视图新增稳定排序（默认、优先级、截止时间、标题、创建时间），旧视图自动迁移为默认顺序；非默认排序不暴露 Today 拖动，排序只作用于当前快照，不进入任务操作或飞书同步。

> v1.36 增量：任务详情新增可折叠研究卡，支持来源链接、摘要和行动项；研究卡加入任务历史、搜索和安全导入导出，private/strict 脱敏清空；Agent 通过独立 R1 工具添加研究卡，FeishuTaskAdapter 保持该字段为本地私人上下文。新增研究卡任务服务、Agent 工具和 Electron 场景均已覆盖。

> v1.37 增量：时间线周视图新增本地工作周期投影，支持 1 周 / 2 周切换、前后周期导航、已安排容量、剩余/超载分钟、周期内完成与待排候选；工作周期复用现有任务日期与估时，不新增 Task 字段、IPC 或飞书写回。

> v1.38 增量：Agent 新增 `task_split` R2 工具，限制 2–7 个唯一步骤，确认后创建本地 `parentId` 子任务；父任务、飞书同步状态和远端 payload 不变，结果返回逐项任务与 operation ID。

> v1.95 增量：Todo Pet 小窝成长页新增 `pet-completion-stamps.ts` 与 `PetCompletionStampsCard`，从任务快照投影最多 12 枚最近完成印章和今日数量；今日完成用星章突出，点击可回到原任务，宠物奖励尚未对账时显示“成长记录同步中”。这是只读投影，不创建任务/奖励副本、不重复计算成长、不写入飞书。

> v1.39 增量：宠物日记新增可选 `taskIds` 本地关系。生成日记时记录当天完成任务，日记卡显示可点击任务胶囊并回到原任务；历史任务已移除时给出明确占位，不复制任务、不写入飞书。

> v1.40 增量：季节事件开关现在会按本地日期给 Todo Pet 添加春花、夏帽、秋叶或冬围巾，并展示可关闭的温和提示；只改变宠物表现，不写任务、成长或飞书字段。

> v1.41 增量：任务新增本地手动情境（办公室、家、出门等），支持任务详情/新建任务录入、列表筛选和保存视图；情境值经过长度与重复校验，行内显示胶囊，搜索可命中；不申请定位、不进入飞书共享载荷。

> v1.42 增量：快速捕获支持 `@办公室` 与 `情境：办公室` Smart Add 语法，解析结果以可编辑情境 chip 呈现并随任务保存；不触发定位、不改变飞书共享载荷。

> v1.43 增量：快速捕获支持 `预计 45 分钟`、`用时 1 小时` 和 `30m/1h` 的预计时长语法，标准化为 5–720 分钟并写入本地 `estimatedMinutes`；预览先显示时长 chip，模板任务仍保留各步骤自己的估时，不静默创建时间块或修改飞书共享字段。

> v1.44 增量：快速捕获支持 `每天`、`每个工作日`、`每周一/三/五`、`每月15日` 和 `每隔 2 周` 的有限循环语法，标准化为本地 `RecurrenceRule` 并显示循环 chip；仅非模板本地任务写入循环，飞书任务不静默改写远端重复规则。

> v1.45 增量：晨间简报新增“只剩 2 小时？”只读替代计划；复用每日规划器按截止风险、优先级、依赖和估时挑选最多 3 项，显示依据与总分钟数，点击后进入已有今日规划确认，不自动改动任务。

> v1.47 增量：设置页新增可读 Markdown 任务清单导出；任务详情新增“写入宠物日记”，由主进程原子创建本地共同经历条目并保留 `taskIds` 原任务关系，重复点击幂等且不带入私人备注。

约束式今日规划已在 v1.48 接入：Today 规划面板可调整可用起止时段、过渡缓冲和最小连续工作块；规划器以有效容量生成建议，并对短任务给出可解释原因。v1.49 增加 Todo Pet 可执行下一步卡：从任务事实确定性选择一项可开始任务，解释原因并提供开始专注/查看任务入口。v1.50 增加确认前只读时间块预览：保留已有时间块，按窗口、估时与缓冲排布其余任务并标记冲突/无空档。v1.51 增加未来 5 个工作日的只读顺延预览，尊重工作日、截止日和固定块。v1.52 增加今晚回顾中的“安排明天”：同一规划面板可切换未来目标日期，确认后原子写入私人 `plannedDate` 并支持撤销，过期日期拒绝。v1.53 增加暂存逐项整理：未安排日期、项目或清单的开放任务可以逐项安排到今天/明天、稍后处理、完成或打开详情；“稍后”只改变当前整理会话，其他动作沿用任务更新、完成、撤销和飞书同步。v1.54 增加宠物任务搬运目标区：任务卡拖动时显示专注 / 完成 / 稍后三个目标，放下后操作原任务，不复制影子任务。v1.55 增加任务详情依赖链解释卡：从同一任务快照投影前置、当前和后续节点，按深度排序并提示缺失/循环关系；点击节点只导航到原任务。v1.56 增加时间线周视图专注节奏分析卡：从任务本地专注记录投影 7 日投入、段数、平均时长和投入最多任务，点击只导航，不改变任务或飞书。以上能力都只影响本地建议与私人计划，不改变飞书共享字段。

## 3. 已实现的关键安全边界

- 渲染层只可调用类型化白名单接口；不暴露 Node、通用 IPC 或 Electron 对象。
- 飞书 Token、每位用户专属 App Secret 和模型 API Key 使用系统安全存储；普通设置只保留凭据引用，日志、状态、IPC 和导出不含明文凭据。
- 本地任务不会因连接飞书而自动上传；私人计划、排序、专注等字段不进入飞书写入载荷。
- 模型只能提出严格 Schema 的工具调用，不能直接写本地数据；调用在执行前重新核对参数、目标、风险、基础版本和授权状态。
- 关闭任务标题与时间数据范围后，任务只以不透明本地 ID 和 `redacted` 标记进入模型；附件元数据与文本当前一律不进入模型。
- 网页、HTTP 和网络命令输出会被标记为不可信；同一轮研究后不能直接执行写入、命令或外部动作，必须等待用户下一条消息明确确认。
- 批量、外部和高风险动作需要确认或命中有效的精确临时授权；R4 禁区在全权限模式下也拒绝。
- 文件工具固定在 Agent 工作区内，并校验规范路径、符号链接和硬链接；网页读取阻止凭据 URL、私网目标和未复核重定向。
- 全局停止会阻止新的 Agent 工具调用并撤销临时全权限，但不停止本地提醒和飞书一致性队列。

这些边界已有单元/集成测试覆盖，但发布前仍应在实际打包应用、macOS 权限弹窗和 Windows 安全环境中重复验证。

## 4. 当前明确不应宣称完成的能力

以下能力在 PRD 中有目标或预留，但当前版本不应出现在发布宣传的“已支持”清单中：

- 独立可交互 Agent 浏览器、用户登录态浏览器和完整网页自动化。
- 容器或虚拟机级终端沙箱，以及任意宿主机权限的 OpenClaw 等价能力。
- MCP、Skills/插件、多 Agent、远程设备、桌面应用控制、邮件/日历/消息连接器。
- 可长期后台运行的自定义无人值守 Agent 工作流。
- 自动形成的长期关系记忆和语义检索。当前版本只保存用户明确批准的可控记忆；本地模型与主/备用路由已实现，但仍需真实模型矩阵验收。
- 产品账号、跨设备云同步、完整移动端和移动推送。
- 经过签名、公证、自动更新和双平台全矩阵验证的公开发行版。

## 5. 验收证据与门禁

### 5.1 当前自动化基线

> v2.06 本轮增量回归：新增 `tray-task-preview.ts` 的只读 Today 投影与隐私脱敏测试；完整单元/集成回归以本轮最终命令输出为准，未启动会弹窗的 Electron 桌面 E2E。

> v1.67 本轮最新回归：96 个 Vitest 文件、686/686 单元与集成测试通过；新增 Boss Mode 设置投影测试，覆盖隐藏宠物、会议模式、托盘可恢复和无关设置保持；v1.66 的晨间三步启动卡、规划弹窗预选与固定/延续任务保留，以及 v1.65 的命令面板、v1.64 的宠物任务栏自然语言解析与 `p1`–`p4`、v1.63 的任务 / 暂存 / 日记去向、captureId 幂等、v1.62 的可读 Markdown 事件摘要、Filter Assist、Agent 入口、规划、宠物与同步边界继续保持。

本轮完整 Playwright 回归共 43 个场景：**41/41 通过、2 项平台/外部模型条件跳过**；其中 `tests/e2e/app.spec.ts` 为 **37/37 通过**，新增研究卡、子任务进度、规划、时间线和工作周期场景均已通过。

v1.66 本轮最终单元与集成回归为 **684/684 通过**；新增晨间启动卡的三步状态、最多三项限制、容量预设、开始第一项和空任务回退覆盖，并验证规划弹窗接收预选容量/任务且保留固定与延续任务；继续覆盖 v1.65 命令面板组件的焦点、关键词过滤、循环导航、Enter 执行、Escape/遮罩关闭，以及规划、宠物和同步安全边界。

v1.67 之后的最新数字以本节前两段为准：本轮未启动 Electron 桌面 E2E，避免弹窗打扰；Boss Mode 纯设置投影、晨间启动卡、命令面板组件、宠物输入解析与 `p1`–`p4`、快速捕获三种去向、暂存字段清理、日记 captureId 幂等、Markdown 事件摘要、Filter Assist 的解析规则、Agent 入口、规划、宠物和同步边界均以 Vitest 或纯函数测试覆盖，类型检查、生产构建和差异检查均已通过。

以下长段落保留此前 v1.47 的隔离回归历史；不要将其中的旧测试数字视为 v1.49 当前基线。

> 说明：下方较早的 129 个文件 / 804 个测试数字是 v2.05 历史快照；当前回归基线以本轮 v2.36 增量的 151 个文件 / 895 个测试为准。

当前交付回归基线为 **151 个 Vitest 文件、895/895 通过**；下方长段的旧数字仅用于保留历史演进记录，不代表当前状态。

v2.15–v2.36 追加能力：隐私优先的工作时回应、低压力休假模式、本机最近捕获历史、显式保存的最近上下文回用、Agent 五层能力开关、工具注册表过滤、运行中撤权校验、对话内可信 Agent 执行过程卡、覆盖 Agent/晨间简报/宠物状态气泡的本机显式语音输出、Agent 回复复制 Markdown、主 Agent 与宠物聊天上下文回用、Agent 回复行动项提取预览与本地任务创建、将完成的 Agent 研究回复确认保存到当前任务私人研究卡、研究卡 Markdown 可读渲染、研究卡行动项带 provenance 创建本地任务、从任务详情保存受限本地工作流模板和可选父子步骤复用、Todoist / Notion / ClickUp 风格的批量属性编辑与单次撤销、Todoist / Microsoft To Do 风格的重复任务“跳过本次”与原子平移/撤销、Agent 对该操作的预览和执行工具，以及 OmniFocus / Taskwarrior 风格的本地重点标记、重点筛选、保存视图、Pet 视图、批量编辑和 Agent CRUD 接入。

本轮 v2.36 最新回归：**151 个 Vitest 文件、895/895 通过**；下方较长的历史段落仍保留旧版本数字，仅用于追溯演进。

仓库当前包含 145 个 Vitest 单元/集成测试文件和 6 个 Electron Playwright 场景文件。2026-08-21 最新一次未启动 Electron 的全量回归确认 **864/864 单元与集成测试通过**；任务讨论、任务拆分、宠物日记关系、季节事件、手动情境、Quick Capture 情境、预计时长与循环语法、批量操作、保存视图排序、子任务进度、工作周期、可读 Markdown 导出、任务写入宠物日记、Todo Pet 档案迁移、专注守护与引导式回顾、模型价格/费用预算、本机短期 Agent 会话、主 Agent 快捷回答建议、会后跟进草稿、会议行动项提取预览、日历忙碌区投影、估时复盘、Agent 执行过程卡、Agent Markdown 回复复制、最近上下文跨入口回用和 Agent 回复行动项提取预览新增场景已由 Vitest 覆盖；本轮没有启动会弹窗的 Electron 回归。快速捕获和 Todo Pet 相关场景覆盖剪贴板/当前窗口/选中文本预览、内置工作流模板批量创建、隐私模式、悬停离开与点击重新展开；此前的宠物、规划、同步和安装版基线仍需按平台条件重复验收。Todo Pet 覆盖 20 种待机动作、8 类情绪、动作优先级和打断、身体点击分区、白天与夜间行为、主动提醒免打扰、互动与冒险奖励幂等，以及真实渲染中的置顶、缩放、拖动锁定、悬停/离开、任务拖放、外部内容安全预览、专注闭环、小房间、外观持久化、每日冒险、小游戏、回应气泡和流式 Markdown；本轮补充了房间摆件的直接拖动、位置/大小编辑与视觉氛围切换、宠物性格与 Agent 语言的可选联动、时间线半小时刻度、拖动本地时间块、未安排托盘、工作周期与容量预测、项目三列看板、周概览、每周回顾、项目健康、每周 Check-in、宠物回顾队列、前置依赖编辑与循环防护、命名上下文链接、保存筛选视图与排序、优先级原因提示、三种今日规划策略、剪贴板/当前窗口/选中文本上下文预览、内置工作流模板与受限 JSON 自定义模板、弹性习惯、专注环境音、显式语音输入、主题动作包、安全声明式动作包、今日进展/今晚回顾、普通任务提醒每日预算 / 来源策略 / 项目例外 / 同类间隔 / 连续忽略降频、宠物主动陪伴预算、主/备用模型路由、清单实体、受限附件预览、任务详情脱敏历史时间线、任务本地讨论、任务列表批量选择与原子撤销、保存视图标签/情境/日期范围、父任务子任务进度、附件/链接/自定义字段元数据搜索与飞书截止日期不变断言。v1.73 还补充了 Todo Pet 对已保存智能视图的读取、失效回退和稳定过滤排序，v1.74 补充了 Todo Pet 档案导出、导入预览与活动专注保护，v1.75 补充了可配置弹性习惯、旧 localStorage 迁移和旧档案兼容，v1.77 补充了专注守护应用名匹配、设置迁移与导入校验，v1.78 补充了引导式回顾会话的队列顺序和原因解释，v1.79 补充了主/备用模型价格配置、本地费用累计和每日费用硬门禁，v1.80 补充了本机短期 Agent 会话恢复、清除和 Markdown 导出，v1.81 补充了主 Agent 上下文快捷回答建议，v1.82 补充了本机最多 8 个短期 Agent 会话归档、切换、搜索、重命名、置顶、删除和清除当前会话，v1.83 补充了会话标题/正文搜索，v1.84 补充了本机会话重命名与置顶，v1.85 补充了日历会后跟进草稿，v1.86 补充了周视图估时复盘，v1.87 补充了会议 `DESCRIPTION` 行动项提取与批量本地预览，v1.88 补充了半小时日时间线会议忙碌区投影，v2.04 补充了房间伙伴小队协作，v2.05 补充了紧凑桌面天气胶囊与隐私/缓存/严重天气提示，v2.19 补充了对话内可信 Agent 执行过程卡与权限拒绝终态事件。完整动作见 [Todo Pet 动作与互动设计规范](./TODO_PET_ACTION_INTERACTION_SPEC.md)。

本轮未启动 Electron 桌面 E2E，以避免弹窗干扰当前工作；弹性习惯持久化、旧档案兼容、任务写入宠物日记与 Todo Pet 档案迁移的新增覆盖为 Vitest 单元/集成测试，类型检查、生产构建和差异检查均已通过。

v1.94 增量回归：新增 `pet-project-chapters.ts` 与 `PetProjectChapters`，将项目任务投影为最多 4 个共同旅程章节，并补充投影/组件测试；当前全量基线以文档开头的 **772/772** 为准，本轮仍未启动会弹窗的 Electron 桌面 E2E。

```bash
npm run typecheck
npm test
npm run test:e2e
```

当前构建已安装到 `/Users/hx/Applications/Todo Agent.app`，并完成 ad-hoc hardened runtime 签名完整性检查、主进程与两个渲染窗口启动冒烟以及安装版界面检查。上一安装版本保留在 `/Users/hx/Applications/Todo Agent.before-live-pet-20260815-1758.app`。发布产物与 SHA-256：

- `Todo Agent-0.0.1-arm64.dmg`：`831d7f0226b1849cdd1bfa876edb915c7bfc36feef650786299d2719dcddb85b`
- `Todo Agent-0.0.1-arm64-mac.zip`：`116d80c22758facd3badc593f2b6e08ec9cacd951aef3efeb2369da2f0b91195`

上述 macOS 产物已包含本轮 Todo Pet 动作与互动改造。已安装应用沿用原有用户数据目录，因此不会清除任务、设置、飞书凭据引用或宠物成长记录。尚未在 Windows 实机运行；所有自动化证据都不能替代完整的人工发布矩阵或真实飞书授权。

### 5.2 桌面本地验收

- [ ] 首次启动不登录、不连接飞书、不启用 AI，也能创建、编辑、完成、删除、恢复、搜索和重启恢复任务。
- [ ] 快捷键可从其他应用呼出快速录入；冲突时给出明确提示。
- [ ] 主窗口、悬浮窗、托盘/菜单栏、通知动作和 Today 始终指向同一任务实体。
- [ ] 到期、稍后、完成、安静时段、当天首次晨报、睡眠唤醒和通知去重均通过人工时间测试。
- [ ] 导出不含凭据；三种导入策略和选择性清除均在副本数据上完成恢复演练。
- [ ] 应用异常退出后任务文件、草稿、撤销记录、提醒状态和待同步队列无静默丢失。

### 5.3 飞书真实环境门禁

默认路径准备一个允许创建自建应用的真实飞书账号；同步能力验收再准备至少两个账号和不同任务角色。当前尚未执行以下真实浏览器授权：

- [ ] 第一条验证链接实际创建当前用户专属应用；显式用户权限为 Task v2 读写与离线授权，并记录飞书最小基座实际附带能力；租户管理员策略和拒绝路径文案正确。
- [ ] 客户端安全保存专属 App Secret 后自动打开第二条 Device OAuth URL；授权、刷新、撤权、重新授权和断开连接均成功。
- [ ] 重启后复用已有专属应用，重新授权不重复创建；普通设置、状态、日志、IPC 和导出不含 Secret 或 Token。
- [ ] 标题、描述、开始、截止、完成/重开、创建和删除的双向写入，与飞书界面逐项一致。
- [ ] 离线编辑跨重启保留；恢复网络后幂等收敛，不重复创建。
- [ ] 同一字段并发修改进入冲突，不静默覆盖；三种处理结果都正确。
- [ ] 无权限、Token 失效、限流、远端删除、分页和增量游标行为可恢复。
- [ ] 单负责人、会签、或签、关注人和只读成员形成带接口版本的能力矩阵；会签个人完成因 OpenAPI 不支持而正确禁用并跳转飞书。
- [ ] 若准备公开支持高级 Relay 模式，再用可信 HTTPS Relay 单独验证授权、刷新、撤权和服务端密钥隔离；Relay 不阻塞默认零服务器路径验收。

在这组门禁完成前，只能表述为“飞书零服务器连接与同步流程已实现并通过 Mock 自动化，等待真实账号/租户验收”，不能表述为“真实飞书登录已打通”或“飞书全部能力已验证”。详细范围见 [FEISHU_CONNECTION.md](./FEISHU_CONNECTION.md)。

### 5.4 真实模型门禁

已使用隔离临时配置连接 `http://10.30.0.21:8005/v1` 的 `DeepSeek-V4-Flash-0731`。真实端到端测试已通过：连接与精确 Token 回报、自然语言只读查询、自然语言创建本地任务、原生多轮 `tool_calls`、R2 批量更新预览与单次批准、结果落库，以及只读 AI 晨间简报。测试任务随临时配置清除，未写入用户真实任务库。由于模型会偶尔省略值为空的工具字段，工具说明已明确要求所有必填字段及显式 `null`，本地严格 Schema 和权限判断保持不变。

为每个准备支持的 OpenAI-compatible 提供方分别验证：

- [ ] Key 保存、替换和删除；请求只发送到用户确认的 Endpoint Origin。
- [ ] 普通对话、任务查询、单条修改、批量预览、拒绝、仅本次批准、临时全权限和停止运行。
- [ ] 原生 `tool_calls` 多轮回传、非法参数、未知工具、重复调用 ID、超时、429、5xx、断网和额度不足。
- [ ] 关闭备注、飞书内容、附件文本或聊天历史后，后续请求立即裁剪相应字段。
- [ ] 明文 Key 不出现在提示、错误、日志、审计、导出或截图中。
- [ ] 网页提示注入不能扩权、修改审计、读取密钥或把不可信内容直接转成写入/命令参数。

### 5.5 发布门禁

- [ ] macOS 13+ 的 Apple Silicon 与 Intel 安装、首次打开、权限恢复、升级和卸载保留数据。
- [ ] Windows 10 22H2 与 Windows 11 x64 的安装、托盘、通知、高 DPI、多显示器、开机启动和卸载。
- [ ] macOS 签名及 notarization、Windows 代码签名均由正式发布凭据验证。
- [ ] 快捷录入、悬浮展开、CPU、内存、同步延迟、提醒误差达到 PRD 非功能指标。
- [ ] 键盘全流程、屏幕阅读器、200% 字体、高对比度、减少动态和关闭透明效果通过人工验收。
- [ ] 安全、隐私、依赖和许可证清单完成发布审查，无已知静默数据丢失或静默覆盖。

## 6. 建议的后续顺序

1. **封板桌面本地核心**：补齐仍暴露在 UI 中的 P0 任务字段和提醒动作，完成数据恢复、性能与无障碍验收。
2. **完成外部门禁**：真实 DeepSeek 基线已跑通；下一步用真实飞书账号完成专属应用创建、Device OAuth 和 Task v2 读写，Relay 仅在决定公开支持高级兼容模式时单独验收；同时扩展更多模型提供方的错误与长会话矩阵。
3. **收紧广权限 Agent**：决定 P0 是明确降级为“限定工具集”，还是补建独立浏览器与真正的终端沙箱；发布文案必须与实现一致。
4. **完成桌面分发**：签名、公证、Windows 实机和安装升级矩阵通过后进入公开 Beta。
5. **再启动移动端**：先建设账号、端到端同步、推送和冲突协议，再做 iOS/Android 的 Today、捕获、通知、Agent 对话和待批准操作；不要直接移植桌面悬浮窗交互。
